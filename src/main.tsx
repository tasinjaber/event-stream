import React,{useMemo,useState}from"react";import{createRoot}from"react-dom/client";import{Activity,Bell,LayoutDashboard,Plus,Search,Settings,Workflow}from"lucide-react";import"./style.css";const metrics=[["Events/sec","842"],["Processed","12.8M"],["Latency","84 ms"],["Failures","0.08%"]],rows=["evt_9f82 · order.created · Order service · Processed","evt_9f81 · payment.captured · Billing · Processed","evt_9f80 · stock.reserved · Inventory · Processed"];function App(){const[q,setQ]=useState(""),[open,setOpen]=useState(false),list=useMemo(()=>rows.filter(x=>x.toLowerCase().includes(q.toLowerCase())),[q]);return <div className="app"><aside><h2>✦ Event Stream</h2><button className="active"><LayoutDashboard/>Overview</button><button><Activity/>Streams</button><button><Workflow/>Consumers</button><button><Settings/>Settings</button><small>OBSERVABILITY</small><p>Event log</p><p>Failures</p><p>Metrics</p></aside><main><header>Streaming / Overview<div><Bell/><b>TJ</b></div></header><section><div className="hero"><div><small>MONDAY · SEPTEMBER 21</small><h1>Event Stream</h1><p>Event-driven order processing</p></div><button className="primary" onClick={()=>setOpen(true)}><Plus/>Create stream</button></div><div className="metrics">{metrics.map(m=><article><span>{m[0]}</span><strong>{m[1]}</strong><small>+8.4% this period</small></article>)}</div><article className="panel"><div className="head"><div><h3>Recent events</h3><p>Inspect event flow and processing health.</p></div><div className="search"><Search/><input placeholder="Search events" value={q} onChange={e=>setQ(e.target.value)}/></div></div>{list.map(x=><div className="row"><b>{x.split(" · ")[0]}</b><span>{x.split(" · ").slice(1,-1).join(" · ")}</span><em>{x.split(" · ").at(-1)}</em></div>)}</article></section></main>{open&&<div className="modal"><div><h2>Create stream</h2><p>Configure a new event stream.</p><input placeholder="Stream name"/><button className="primary" onClick={()=>setOpen(false)}>Create stream</button></div></div>}</div>}createRoot(document.getElementById("root")!).render(<React.StrictMode><App/></React.StrictMode>);